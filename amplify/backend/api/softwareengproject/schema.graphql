enum Priority {
  LOW
  NORMAL
  HIGH
}

type Todo @model {
  id: ID!
  name: String!
  priority: Priority
  description: String
}

type UserLocation @model {
  id: ID!
  username: String
  latitude: Float
  longitude: Float
  location: String
  time: String
}